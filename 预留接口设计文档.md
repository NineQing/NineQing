# 电竞陪玩护航系统 - 预留接口设计文档

## 概述

本文档详细描述电竞陪玩护航系统MVP阶段暂不实现但已预留接口的功能模块，包括营销功能、会员体系、社交功能、扩展配置等后续迭代功能的接口设计规范，预留接口遵循与现有接口一致的设计规范，便于后续快速开发和集成。

---

## 一、预留接口总览

### 1.1 功能模块清单

| 模块名称 | 模块标识 | 预留功能 | 开发优先级 | 预估工时 | 说明 |
|----------|----------|----------|------------|----------|------|
| 营销工具 | marketing | 优惠券、满减活动、订单抽奖 | P1 | 3天 | 提升用户转化 |
| 会员体系 | vip | 会员等级、会员权益、折扣 | P1 | 3天 | 提升用户粘性 |
| 社交功能 | social | 动态广场、关注、话题 | P2 | 5天 | 增强社交属性 |
| 工作室 | studio | 工作室管理、打手分组 | P2 | 4天 | 支持团队运营 |
| 管事推广 | manager | 邀请码、邀请收益 | P2 | 3天 | 分销推广体系 |
| 扩展配置 | extend | 大区管理、服务器定价 | P1 | 2天 | 精细化运营 |

### 1.2 接口设计原则

预留接口的设计遵循以下核心原则：与现有接口保持一致的返回格式和错误处理机制，确保前端调用方式的统一性；使用功能开关控制接口的启用和禁用，便于灰度发布和快速回滚；预留充足的数据字段和扩展字段，避免后续版本的数据迁移；接口路径遵循RESTful规范，便于理解和维护。

---

## 二、营销功能模块

### 2.1 优惠券功能

#### 2.1.1 优惠券类型定义

| 类型 | 类型值 | 说明 | 示例 |
|------|--------|------|------|
| 满减券 | reduce | 满X减Y | 满100减20 |
| 折扣券 | discount | X折 | 8折券 |
| 代金券 | cash | 抵用X元 | 10元代金券 |
| 新人券 | new_user | 仅新用户使用 | 首单立减 |
| 商品券 | product | 指定商品使用 | 指定商品专用 |

#### 2.1.2 优惠券相关接口

##### 2.1.2.1 获取优惠券列表

**接口路径**

```
GET /api/v1/marketing/coupon/list
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | string | 否 | 优惠券类型筛选 |
| status | int | 否 | 状态：0未领取，1已领取，2已使用，3已过期 |
| page | int | 否 | 页码，默认1 |
| page_size | int | 否 | 每页数量，默认20 |

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1001,
        "name": "新人专享券",
        "type": "reduce",
        "type_text": "满减券",
        "threshold_amount": "100.00",
        "discount_amount": "20.00",
        "valid_start_time": "2024-01-01 00:00:00",
        "valid_end_time": "2024-12-31 23:59:59",
        "status": 0,
        "receive_limit": 1,
        "received_count": 5000,
        "total_count": 10000,
        "is_received": false,
        "description": "新用户首单满100减20"
      }
    ],
    "total": 50,
    "page": 1,
    "page_size": 20
  }
}
```

##### 2.1.2.2 领取优惠券

**接口路径**

```
POST /api/v1/marketing/coupon/receive
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| coupon_id | int | 是 | 优惠券ID |

**响应参数**

```json
{
  "code": 200,
  "message": "领取成功",
  "data": {
    "coupon_id": 1001,
    "receive_time": "2024-01-15 10:30:00",
    "expire_time": "2024-01-31 23:59:59"
  }
}
```

##### 2.1.2.3 我的优惠券

**接口路径**

```
GET /api/v1/marketing/coupon/my
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| status | int | 否 | 状态：0未使用，1已使用，2已过期 |
| page | int | 否 | 页码 |
| page_size | int | 否 | 每页数量 |

##### 2.1.2.4 下单时获取可用优惠券

**接口路径**

```
GET /api/v1/marketing/coupon/available
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| product_id | int | 是 | 商品ID |
| amount | decimal | 是 | 订单金额 |

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1001,
        "name": "满100减20券",
        "type": "reduce",
        "discount_amount": "20.00",
        "is_best": true,
        "expire_time": "2024-01-31 23:59:59"
      }
    ],
    "best_coupon": {
      "id": 1001,
      "name": "满100减20券",
      "discount_amount": "20.00"
    },
    "can_use_count": 3,
    "total_saving": "40.00"
  }
}
```

### 2.2 满减活动功能

#### 2.2.1 满减活动相关接口

##### 2.2.1.1 获取满减活动列表

**接口路径**

```
GET /api/v1/marketing/activity/list
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | string | 否 | 活动类型：full_reduce满减，gift赠品 |
| status | int | 否 | 状态：0未开始，1进行中，2已结束 |

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1001,
        "name": "春节特惠满减",
        "type": "full_reduce",
        "start_time": "2024-01-20 00:00:00",
        "end_time": "2024-02-10 23:59:59",
        "rules": [
          {"threshold_amount": "100.00", "discount_amount": "10.00"},
          {"threshold_amount": "200.00", "discount_amount": "30.00"},
          {"threshold_amount": "500.00", "discount_amount": "80.00"}
        ],
        "status": 1,
        "description": "全场满100减10，满200减30，满500减80"
      }
    ]
  }
}
```

### 2.3 订单抽奖功能

#### 2.3.1 抽奖相关接口

##### 2.3.1.1 订单完成后抽奖

**接口路径**

```
POST /api/v1/marketing/lottery/draw
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| order_id | int | 是 | 订单ID |

**响应参数**

```json
{
  "code": 200,
  "message": "抽奖成功",
  "data": {
    "is_win": true,
    "prize": {
      "id": 2001,
      "name": "5元代金券",
      "type": "coupon",
      "coupon_id": 2001,
      "value": "5.00"
    },
    "remain_times": 2,
    "lottery_times": 3
  }
}
```

##### 2.3.1.2 获取抽奖机会

**接口路径**

```
GET /api/v1/marketing/lottery/chances
```

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total_times": 5,
    "used_times": 2,
    "remain_times": 3,
    "expire_times": 1,
    "details": [
      {"source": "order", "order_no": "EG202401151234567890", "times": 1, "used": false, "expire_time": "2024-01-31 23:59:59"}
    ]
  }
}
```

---

## 三、会员体系模块

### 3.1 会员等级定义

| 等级 | 等级值 | 名称 | 成长值要求 | 权益折扣 |
|------|--------|------|------------|----------|
| LV1 | 1 | 普通会员 | 0-999 | 无折扣 |
| LV2 | 2 | 银牌会员 | 1000-4999 | 9.8折 |
| LV3 | 3 | 金牌会员 | 5000-19999 | 9.5折 |
| LV4 | 4 | 钻石会员 | 20000-49999 | 9折 |
| LV5 | 5 | 至尊会员 | 50000+ | 8.5折 |

### 3.2 会员相关接口

#### 3.2.1 获取会员信息

**接口路径**

```
GET /api/v1/vip/info
```

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "level": 3,
    "level_name": "金牌会员",
    "growth_value": 8000,
    "next_level_value": 20000,
    "progress": 40,
    "discount": 0.95,
    "benefits": {
      "order_discount": 0.95,
      "recharge_gift": 1.05,
      "withdraw_priority": true,
      "专属客服": true,
      "专属活动": true
    },
    "expire_time": null,
    "is_permanent": true
  }
}
```

#### 3.2.2 获取会员权益

**接口路径**

```
GET /api/v1/vip/benefits
```

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "levels": [
      {
        "level": 1,
        "name": "普通会员",
        "growth_required": 0,
        "benefits": ["基础服务"],
        "discount": 1.0
      },
      {
        "level": 2,
        "name": "银牌会员",
        "growth_required": 1000,
        "benefits": ["9.8折优惠", "充值赠送5%"],
        "discount": 0.98
      }
    ],
    "upgrade_rules": "成长值=消费金额×1，每完成1笔订单可获得对应成长值"
  }
}
```

#### 3.2.3 成长值记录

**接口路径**

```
GET /api/v1/vip/growth/records
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | int | 否 | 类型：1获得，2消耗 |
| page | int | 否 | 页码 |
| page_size | int | 否 | 每页数量 |

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1001,
        "type": 1,
        "type_text": "获得",
        "change_value": 30,
        "source": "order",
        "order_no": "EG202401151234567890",
        "created_at": "2024-01-15 10:30:00"
      }
    ],
    "total": 100,
    "page": 1,
    "page_size": 20
  }
}
```

---

## 四、社交功能模块

### 4.1 动态广场功能

#### 4.1.1 发布动态

**接口路径**

```
POST /api/v1/social/post/create
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| content | string | 是 | 动态内容（最多500字） |
| images | array | 否 | 图片列表（最多9张） |
| topic_id | int | 否 | 话题ID |
| at_users | array | 否 | @的用户ID列表 |
| location | object | 否 | 位置信息 |

**请求示例**

```json
{
  "content": "今天上分成功，开心！",
  "images": [
    "https://cdn.escortgame.com/post/1.png",
    "https://cdn.escortgame.com/post/2.png"
  ],
  "topic_id": 1001,
  "at_users": [2001, 2002],
  "location": {
    "name": "上海市",
    "latitude": 31.2304,
    "longitude": 121.4737
  }
}
```

#### 4.1.2 动态列表

**接口路径**

```
GET /api/v1/social/post/list
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | string | 否 | 类型：follow关注，hot热门，latest最新 |
| topic_id | int | 否 | 话题ID筛选 |
| user_id | int | 否 | 用户ID筛选 |
| page | int | 否 | 页码 |
| page_size | int | 否 | 每页数量 |

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 5001,
        "user": {
          "id": 2001,
          "nickname": "游戏达人",
          "avatar": "https://cdn.escortgame.com/avatar/1.png",
          "level": 5,
          "is_followed": true
        },
        "content": "今天上分成功，开心！",
        "images": [
          {"url": "https://cdn.escortgame.com/post/1.png", "width": 750, "height": 1000}
        ],
        "topic": {
          "id": 1001,
          "name": "#今日上分#"
        },
        "location": {
          "name": "上海市"
        },
        "like_count": 128,
        "comment_count": 32,
        "share_count": 5,
        "is_liked": false,
        "created_at": "2024-01-15 10:30:00",
        "time_text": "3小时前"
      }
    ],
    "total": 1000,
    "page": 1,
    "page_size": 20
  }
}
```

#### 4.1.3 点赞动态

**接口路径**

```
POST /api/v1/social/post/like
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| post_id | int | 是 | 动态ID |

#### 4.1.4 评论动态

**接口路径**

```
POST /api/v1/social/comment/create
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| post_id | int | 是 | 动态ID |
| content | string | 是 | 评论内容（最多200字） |
| reply_id | int | 否 | 回复的评论ID |

### 4.2 关注功能

#### 4.2.1 关注用户

**接口路径**

```
POST /api/v1/social/follow
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| user_id | int | 是 | 被关注用户ID |

#### 4.2.2 获取关注列表

**接口路径**

```
GET /api/v1/social/following/list
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| user_id | int | 否 | 用户ID，默认当前用户 |
| page | int | 否 | 页码 |
| page_size | int | 否 | 每页数量 |

#### 4.2.3 获取粉丝列表

**接口路径**

```
GET /api/v1/social/follower/list
```

### 4.3 话题功能

#### 4.3.1 话题列表

**接口路径**

```
GET /api/v1/social/topic/list
```

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1001,
        "name": "#今日上分#",
        "description": "分享你今天的上分时刻",
        "cover_image": "https://cdn.escortgame.com/topic/1.png",
        "post_count": 50000,
        "follow_count": 10000,
        "is_followed": false
      }
    ],
    "hot_topics": [
      {"id": 1001, "name": "#今日上分#", "post_count": 50000}
    ]
  }
}
```

---

## 五、工作室模块

### 5.1 工作室相关接口

#### 5.1.1 创建工作室

**接口路径**

```
POST /api/v1/studio/create
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 工作室名称 |
| description | string | 否 | 工作室简介 |
| avatar | string | 否 | 工作室头像 |
| dividend_rate | decimal | 是 | 分成比例（0.5-0.9） |

#### 5.1.2 工作室详情

**接口路径**

```
GET /api/v1/studio/detail
```

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1001,
    "name": "王者工作室",
    "description": "专业代练团队",
    "avatar": "https://cdn.escortgame.com/studio/1.png",
    "owner": {
      "id": 2001,
      "nickname": "工作室主",
      "avatar": "https://cdn.escortgame.com/avatar/1.png"
    },
    "dividend_rate": 0.8,
    "member_count": 20,
    "today_order_count": 50,
    "today_income": "5000.00",
    "month_income": "100000.00"
  }
}
```

#### 5.1.3 工作室成员列表

**接口路径**

```
GET /api/v1/studio/members
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| studio_id | int | 是 | 工作室ID |
| status | int | 否 | 状态：0审核中，1正常，2已退出 |

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 3001,
        "user": {
          "id": 2002,
          "nickname": "大神A",
          "avatar": "https://cdn.escortgame.com/avatar/2.png",
          "level": 8
        },
        "role": "member",
        "role_text": "成员",
        "dividend_rate": 0.7,
        "today_order_count": 5,
        "today_income": "500.00",
        "status": 1,
        "join_time": "2024-01-01 10:00:00"
      }
    ],
    "stats": {
      "total_count": 20,
      "active_count": 18,
      "total_income": "50000.00"
    }
  }
}
```

#### 5.1.4 设置成员分成

**接口路径**

```
POST /api/v1/studio/member/divide
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| member_id | int | 是 | 成员ID |
| dividend_rate | decimal | 是 | 分成比例（0.5-0.9） |

---

## 六、管事推广模块

### 6.1 邀请相关接口

#### 6.1.1 获取邀请信息

**接口路径**

```
GET /api/v1/manager/info
```

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "invite_code": "ABC123",
    "invite_url": "https://escortgame.com/register?code=ABC123",
    "qrcode": "https://cdn.escortgame.com/qrcode/invite_ABC123.png",
    "stats": {
      "total_invite": 50,
      "valid_invite": 30,
      "total_reward": "1500.00",
      "pending_reward": "200.00"
    }
  }
}
```

#### 6.1.2 邀请记录

**接口路径**

```
GET /api/v1/manager/invite/records
```

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1001,
        "user": {
          "id": 3001,
          "nickname": "被邀请用户",
          "avatar": "https://cdn.escortgame.com/avatar/3.png"
        },
        "status": 1,
        "status_text": "有效用户",
        "first_order_time": "2024-01-10 10:00:00",
        "first_order_amount": "100.00",
        "reward_amount": "10.00",
        "reward_status": 1,
        "created_at": "2024-01-05 10:00:00"
      }
    ],
    "stats": {
      "total_count": 50,
      "valid_count": 30,
      "total_reward": "1500.00"
    }
  }
}
```

#### 6.1.3 邀请收益明细

**接口路径**

```
GET /api/v1/manager/reward/records
```

### 6.2 邀请分成规则

| 奖励类型 | 奖励规则 | 说明 |
|----------|----------|------|
| 首单奖励 | 被邀请人首单金额×5% | 最低5元，最高50元 |
| 累计奖励 | 被邀请人累计消费×2% | 上不封顶 |
| 团队奖励 | 团队成员订单×1% | 管事下线成员的订单 |

---

## 七、扩展配置模块

### 7.1 大区管理功能

#### 7.1.1 获取大区列表

**接口路径**

```
GET /api/v1/extend/region/list
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| category_id | int | 是 | 游戏分类ID |

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1001,
      "name": "微信区",
      "category_id": 1,
      "sort": 1,
      "status": 1,
      "servers": [
        {"id": 2001, "name": "王者", "region_id": 1001},
        {"id": 2002, "name": "巅峰", "region_id": 1001}
      ]
    }
  ]
}
```

#### 7.1.2 大区定价

**接口路径**

```
GET /api/v1/extend/region/pricing
```

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| category_id | int | 是 | 游戏分类ID |
| region_id | int | 否 | 大区ID |

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "base_price": "30.00",
    "region_multiplier": {
      "1001": 1.0,
      "1002": 1.2
    },
    "server_multiplier": {
      "2001": 1.0,
      "2002": 1.1
    }
  }
}
```

### 7.2 客服分组功能

#### 7.2.1 获取客服列表

**接口路径**

```
GET /api/v1/extend/service/list
```

**响应参数**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1001,
        "name": "小助手",
        "avatar": "https://cdn.escortgame.com/service/1.png",
        "status": "online",
        "status_text": "在线",
        "waiting_count": 5
      }
    ],
    "queue_position": 3,
    "estimated_time": "5分钟"
  }
}
```

---

## 八、数据模型设计

### 8.1 优惠券表（coupons）

```sql
CREATE TABLE `coupons` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `name` varchar(50) NOT NULL DEFAULT '' COMMENT '优惠券名称',
  `type` tinyint NOT NULL DEFAULT '1' COMMENT '类型：1满减券2折扣券3代金券4新人券5商品券',
  `threshold_amount` decimal(10,2) NOT NULL DEFAULT '0.00' COMMENT '门槛金额',
  `discount_value` decimal(10,2) NOT NULL DEFAULT '0.00' COMMENT '优惠值',
  `discount_rate` decimal(4,2) NOT NULL DEFAULT '1.00' COMMENT '折扣率',
  `total_count` int NOT NULL DEFAULT '0' COMMENT '总数量',
  `received_count` int NOT NULL DEFAULT '0' COMMENT '已领取数量',
  `per_limit` int NOT NULL DEFAULT '1' COMMENT '每人限领',
  `valid_start_time` datetime DEFAULT NULL COMMENT '生效时间',
  `valid_end_time` datetime DEFAULT NULL COMMENT '失效时间',
  `product_ids` text COMMENT '适用商品ID列表',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '状态：0禁用1启用',
  `created_at` datetime NOT NULL,
  `updated_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_type` (`type`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `user_coupons` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `user_id` bigint unsigned NOT NULL,
  `coupon_id` bigint unsigned NOT NULL,
  `order_id` bigint unsigned DEFAULT NULL COMMENT '使用的订单ID',
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '状态：0未使用1已使用2已过期',
  `receive_time` datetime NOT NULL COMMENT '领取时间',
  `use_time` datetime DEFAULT NULL COMMENT '使用时间',
  `expire_time` datetime NOT NULL COMMENT '过期时间',
  `created_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_user` (`user_id`),
  KEY `idx_coupon` (`coupon_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 8.2 会员表（vip_members）

```sql
CREATE TABLE `vip_members` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `user_id` bigint unsigned NOT NULL,
  `level` tinyint NOT NULL DEFAULT '1' COMMENT '会员等级',
  `growth_value` int NOT NULL DEFAULT '0' COMMENT '成长值',
  `total_consume` decimal(10,2) NOT NULL DEFAULT '0.00' COMMENT '累计消费',
  `total_order_count` int NOT NULL DEFAULT '0' COMMENT '累计订单数',
  `is_permanent` tinyint NOT NULL DEFAULT '0' COMMENT '是否永久会员',
  `expire_time` datetime DEFAULT NULL COMMENT '过期时间',
  `created_at` datetime NOT NULL,
  `updated_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `vip_growth_records` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `user_id` bigint unsigned NOT NULL,
  `type` tinyint NOT NULL DEFAULT '1' COMMENT '类型：1获得2消耗',
  `change_value` int NOT NULL COMMENT '变动值',
  `source` varchar(20) NOT NULL DEFAULT '' COMMENT '来源：order订单、gift礼物',
  `source_id` bigint unsigned DEFAULT NULL COMMENT '来源ID',
  `remark` varchar(255) DEFAULT NULL COMMENT '备注',
  `created_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 8.3 动态表（social_posts）

```sql
CREATE TABLE `social_posts` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `user_id` bigint unsigned NOT NULL,
  `content` varchar(500) NOT NULL DEFAULT '' COMMENT '内容',
  `images` text COMMENT '图片JSON',
  `topic_id` bigint unsigned DEFAULT NULL COMMENT '话题ID',
  `location_name` varchar(50) DEFAULT NULL COMMENT '位置名称',
  `latitude` decimal(10,8) DEFAULT NULL COMMENT '纬度',
  `longitude` decimal(11,8) DEFAULT NULL COMMENT '经度',
  `like_count` int NOT NULL DEFAULT '0' COMMENT '点赞数',
  `comment_count` int NOT NULL DEFAULT '0' COMMENT '评论数',
  `share_count` int NOT NULL DEFAULT '0' COMMENT '分享数',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '状态：0隐藏1正常',
  `created_at` datetime NOT NULL,
  `updated_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_user` (`user_id`),
  KEY `idx_topic` (`topic_id`),
  KEY `idx_created` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `social_follows` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `user_id` bigint unsigned NOT NULL COMMENT '关注者ID',
  `follow_user_id` bigint unsigned NOT NULL COMMENT '被关注者ID',
  `created_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_follow` (`user_id`, `follow_user_id`),
  KEY `idx_follow_user` (`follow_user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `social_comments` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `post_id` bigint unsigned NOT NULL,
  `user_id` bigint unsigned NOT NULL,
  `content` varchar(200) NOT NULL DEFAULT '' COMMENT '评论内容',
  `reply_id` bigint unsigned DEFAULT NULL COMMENT '回复的评论ID',
  `like_count` int NOT NULL DEFAULT '0' COMMENT '点赞数',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '状态',
  `created_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_post` (`post_id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 8.4 工作室表（studios）

```sql
CREATE TABLE `studios` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `owner_id` bigint unsigned NOT NULL COMMENT '创始人ID',
  `name` varchar(50) NOT NULL DEFAULT '' COMMENT '工作室名称',
  `description` varchar(255) DEFAULT NULL COMMENT '工作室简介',
  `avatar` varchar(255) DEFAULT NULL COMMENT '头像',
  `dividend_rate` decimal(3,2) NOT NULL DEFAULT '0.80' COMMENT '分成比例',
  `member_count` int NOT NULL DEFAULT '0' COMMENT '成员数量',
  `total_income` decimal(12,2) NOT NULL DEFAULT '0.00' COMMENT '累计收益',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '状态：0禁用1正常',
  `created_at` datetime NOT NULL,
  `updated_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_owner` (`owner_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `studio_members` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `studio_id` bigint unsigned NOT NULL,
  `user_id` bigint unsigned NOT NULL,
  `role` tinyint NOT NULL DEFAULT '1' COMMENT '角色：1成员2管理员',
  `dividend_rate` decimal(3,2) NOT NULL DEFAULT '0.70' COMMENT '个人分成比例',
  `today_order_count` int NOT NULL DEFAULT '0' COMMENT '今日接单数',
  `today_income` decimal(10,2) NOT NULL DEFAULT '0.00' COMMENT '今日收益',
  `total_income` decimal(12,2) NOT NULL DEFAULT '0.00' COMMENT '累计收益',
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '状态：0审核中1正常2已退出',
  `join_time` datetime NOT NULL COMMENT '加入时间',
  `exit_time` datetime DEFAULT NULL COMMENT '退出时间',
  `created_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_studio_user` (`studio_id`, `user_id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 九、实现优先级建议

### 9.1 第一批迭代（1-2周）

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 优惠券 | P1 | 基础优惠券功能，提升转化 |
| 大区管理 | P1 | 精细化运营必备 |

### 9.2 第二批迭代（3-4周）

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 会员体系 | P1 | 用户留存，提升粘性 |
| 满减活动 | P2 | 营销活动 |

### 9.3 第三批迭代（5-8周）

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 工作室 | P2 | 团队运营支持 |
| 管事推广 | P2 | 分销推广体系 |
| 动态广场 | P3 | 增强社交属性 |

---

*文档版本：v1.0*  
*创建日期：2026-05-21*  
*最后更新：2026-05-21*
