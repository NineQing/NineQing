# 电竞陪玩护航系统 - 数据库设计文档 (MVP版)

## 一、数据库概览

- **数据库名**: escort_game
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_unicode_ci
- **引擎**: InnoDB

---

## 二、数据表设计

### 2.1 用户表 (users)

| 字段名 | 类型 | 长度 | 允许空 | 默认值 | 说明 |
|--------|------|------|--------|--------|------|
| id | bigint | 20 | 否 | | 主键，自增 |
| phone | varchar | 20 | 否 | | 手机号 |
| password | varchar | 255 | 否 | | 密码（加密） |
| nickname | varchar | 50 | 否 | | 昵称 |
| avatar | varchar | 255 | 否 | | 头像URL |
| role | tinyint | 1 | 否 | 1 | 角色：1用户，2大神，3客服，4管理员 |
| balance | decimal | 10,2 | 否 | 0.00 | 账户余额 |
| real_name | varchar | 50 | 是 | | 真实姓名 |
| id_card | varchar | 20 | 是 | | 身份证号 |
| expert_status | tinyint | 1 | 是 | 0 | 大神认证状态：0未认证，1审核中，2已认证，3已拒绝 |
| level | int | 11 | 否 | 1 | 等级 |
| status | tinyint | 1 | 否 | 1 | 状态：0禁用，1正常 |
| created_at | datetime | | 否 | | 创建时间 |
| updated_at | datetime | | 否 | | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- UNIQUE KEY `idx_phone` (`phone`)
- KEY `idx_role` (`role`)

---

### 2.2 游戏分类表 (categories)

| 字段名 | 类型 | 长度 | 允许空 | 默认值 | 说明 |
|--------|------|------|--------|--------|------|
| id | bigint | 20 | 否 | | 主键，自增 |
| name | varchar | 50 | 否 | | 分类名称 |
| icon | varchar | 255 | 否 | | 分类图标 |
| sort | int | 11 | 否 | 0 | 排序 |
| status | tinyint | 1 | 否 | 1 | 状态：0禁用，1启用 |
| created_at | datetime | | 否 | | 创建时间 |

---

### 2.3 商品表 (products)

| 字段名 | 类型 | 长度 | 允许空 | 默认值 | 说明 |
|--------|------|------|--------|--------|------|
| id | bigint | 20 | 否 | | 主键，自增 |
| category_id | bigint | 20 | 否 | | 分类ID |
| expert_id | bigint | 20 | 否 | | 大神ID |
| title | varchar | 100 | 否 | | 商品标题 |
| description | text | | 是 | | 商品描述 |
| price | decimal | 10,2 | 否 | | 价格 |
| unit | varchar | 20 | 否 | | 单位（局/小时/天） |
| cover_image | varchar | 255 | 否 | | 封面图 |
| images | text | | 是 | | 商品图片，JSON格式 |
| sort | int | 11 | 否 | 0 | 排序 |
| status | tinyint | 1 | 否 | 1 | 状态：0下架，1上架 |
| created_at | datetime | | 否 | | 创建时间 |
| updated_at | datetime | | 否 | | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_category` (`category_id`)
- KEY `idx_expert` (`expert_id`)

---

### 2.4 订单表 (orders)

| 字段名 | 类型 | 长度 | 允许空 | 默认值 | 说明 |
|--------|------|------|--------|--------|------|
| id | bigint | 20 | 否 | | 主键，自增 |
| order_no | varchar | 32 | 否 | | 订单号 |
| user_id | bigint | 20 | 否 | | 用户ID |
| product_id | bigint | 20 | 否 | | 商品ID |
| expert_id | bigint | 20 | 是 | | 大神ID |
| title | varchar | 100 | 否 | | 订单标题 |
| amount | decimal | 10,2 | 否 | | 订单金额 |
| quantity | int | 11 | 否 | 1 | 数量 |
| remark | varchar | 500 | 是 | | 用户备注 |
| status | tinyint | 1 | 否 | 0 | 状态：0待支付，1待接单，2进行中，3已完成，4退款中，5已退款，6已取消 |
| pay_type | varchar | 20 | 是 | | 支付方式：wechat/alipay/balance |
| pay_time | datetime | | 是 | | 支付时间 |
| accept_time | datetime | | 是 | | 接单时间 |
| finish_time | datetime | | 是 | | 完成时间 |
| created_at | datetime | | 否 | | 创建时间 |
| updated_at | datetime | | 否 | | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- UNIQUE KEY `idx_order_no` (`order_no`)
- KEY `idx_user` (`user_id`)
- KEY `idx_expert` (`expert_id`)
- KEY `idx_status` (`status`)

---

### 2.5 消息表 (messages)

| 字段名 | 类型 | 长度 | 允许空 | 默认值 | 说明 |
|--------|------|------|--------|--------|------|
| id | bigint | 20 | 否 | | 主键，自增 |
| order_id | bigint | 20 | 是 | | 关联订单ID |
| from_user_id | bigint | 20 | 否 | | 发送者ID |
| to_user_id | bigint | 20 | 否 | | 接收者ID |
| type | tinyint | 1 | 否 | 1 | 消息类型：1文字，2图片 |
| content | text | | 否 | | 消息内容 |
| is_read | tinyint | 1 | 否 | 0 | 是否已读：0否，1是 |
| created_at | datetime | | 否 | | 创建时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_order` (`order_id`)
- KEY `idx_users` (`from_user_id`, `to_user_id`)

---

### 2.6 收益记录表 (income_records)

| 字段名 | 类型 | 长度 | 允许空 | 说明 |
|--------|------|------|--------|--------|
| id | bigint | 20 | 否 | 主键，自增 |
| user_id | bigint | 20 | 否 | 用户ID |
| order_id | bigint | 20 | 是 | 关联订单ID |
| type | tinyint | 1 | 否 | 类型：1收益，2提现，3充值 |
| amount | decimal | 10,2 | 否 | 金额 |
| balance_before | decimal | 10,2 | 否 | 变动前余额 |
| balance_after | decimal | 10,2 | 否 | 变动后余额 |
| remark | varchar | 255 | 是 | 备注 |
| created_at | datetime | | 否 | 创建时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_user` (`user_id`)

---

### 2.7 提现申请表 (withdrawals)

| 字段名 | 类型 | 长度 | 允许空 | 默认值 | 说明 |
|--------|------|------|--------|--------|------|
| id | bigint | 20 | 否 | | 主键，自增 |
| user_id | bigint | 20 | 否 | | 用户ID |
| order_id | bigint | 20 | 否 | | 订单ID |
| amount | decimal | 10,2 | 否 | | 提现金额 |
| account_type | varchar | 20 | 否 | | 账户类型：wechat/alipay/bank |
| account_no | varchar | 100 | 否 | | 账号 |
| real_name | varchar | 50 | 否 | | 真实姓名 |
| status | tinyint | 1 | 否 | 0 | 状态：0待审核，1审核通过，2已拒绝 |
| audit_time | datetime | | 是 | | 审核时间 |
| audit_user_id | bigint | 20 | 是 | | 审核人ID |
| remark | varchar | 255 | 是 | | 备注 |
| created_at | datetime | | 否 | | 创建时间 |
| updated_at | datetime | | 否 | | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_user` (`user_id`)

---

### 2.8 系统配置表 (configs)

| 字段名 | 类型 | 长度 | 允许空 | 默认值 | 说明 |
|--------|------|------|--------|--------|------|
| id | bigint | 20 | 否 | | 主键，自增 |
| key | varchar | 50 | 否 | | 配置键 |
| value | text | | 是 | | 配置值 |
| description | varchar | 255 | 是 | | 配置说明 |
| created_at | datetime | | 否 | | 创建时间 |
| updated_at | datetime | | 否 | | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- UNIQUE KEY `idx_key` (`key`)

---

## 三、初始化SQL

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS `escort_game` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE `escort_game`;

-- 用户表
CREATE TABLE `users` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `phone` varchar(20) NOT NULL DEFAULT '',
  `password` varchar(255) NOT NULL DEFAULT '',
  `nickname` varchar(50) NOT NULL DEFAULT '',
  `avatar` varchar(255) NOT NULL DEFAULT '',
  `role` tinyint NOT NULL DEFAULT '1' COMMENT '1用户2大神3客服4管理员',
  `balance` decimal(10,2) NOT NULL DEFAULT '0.00',
  `real_name` varchar(50) DEFAULT NULL,
  `id_card` varchar(20) DEFAULT NULL,
  `expert_status` tinyint DEFAULT '0' COMMENT '0未认证1审核中2已认证3已拒绝',
  `level` int NOT NULL DEFAULT '1',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '0禁用1正常',
  `created_at` datetime NOT NULL,
  `updated_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_phone` (`phone`),
  KEY `idx_role` (`role`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 游戏分类表
CREATE TABLE `categories` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `name` varchar(50) NOT NULL DEFAULT '',
  `icon` varchar(255) NOT NULL DEFAULT '',
  `sort` int NOT NULL DEFAULT '0',
  `status` tinyint NOT NULL DEFAULT '1',
  `created_at` datetime NOT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 商品表
CREATE TABLE `products` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `category_id` bigint unsigned NOT NULL,
  `expert_id` bigint unsigned NOT NULL,
  `title` varchar(100) NOT NULL DEFAULT '',
  `description` text,
  `price` decimal(10,2) NOT NULL,
  `unit` varchar(20) NOT NULL DEFAULT '',
  `cover_image` varchar(255) NOT NULL DEFAULT '',
  `images` text,
  `sort` int NOT NULL DEFAULT '0',
  `status` tinyint NOT NULL DEFAULT '1',
  `created_at` datetime NOT NULL,
  `updated_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_category` (`category_id`),
  KEY `idx_expert` (`expert_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 订单表
CREATE TABLE `orders` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `order_no` varchar(32) NOT NULL DEFAULT '',
  `user_id` bigint unsigned NOT NULL,
  `product_id` bigint unsigned NOT NULL,
  `expert_id` bigint unsigned DEFAULT NULL,
  `title` varchar(100) NOT NULL DEFAULT '',
  `amount` decimal(10,2) NOT NULL,
  `quantity` int NOT NULL DEFAULT '1',
  `remark` varchar(500) DEFAULT NULL,
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '0待支付1待接单2进行中3已完成4退款中5已退款6已取消',
  `pay_type` varchar(20) DEFAULT NULL,
  `pay_time` datetime DEFAULT NULL,
  `accept_time` datetime DEFAULT NULL,
  `finish_time` datetime DEFAULT NULL,
  `created_at` datetime NOT NULL,
  `updated_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_order_no` (`order_no`),
  KEY `idx_user` (`user_id`),
  KEY `idx_expert` (`expert_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 消息表
CREATE TABLE `messages` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `order_id` bigint unsigned DEFAULT NULL,
  `from_user_id` bigint unsigned NOT NULL,
  `to_user_id` bigint unsigned NOT NULL,
  `type` tinyint NOT NULL DEFAULT '1' COMMENT '1文字2图片',
  `content` text NOT NULL,
  `is_read` tinyint NOT NULL DEFAULT '0',
  `created_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_order` (`order_id`),
  KEY `idx_users` (`from_user_id`, `to_user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 收益记录表
CREATE TABLE `income_records` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `user_id` bigint unsigned NOT NULL,
  `order_id` bigint unsigned DEFAULT NULL,
  `type` tinyint NOT NULL COMMENT '1收益2提现3充值',
  `amount` decimal(10,2) NOT NULL,
  `balance_before` decimal(10,2) NOT NULL,
  `balance_after` decimal(10,2) NOT NULL,
  `remark` varchar(255) DEFAULT NULL,
  `created_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 提现申请表
CREATE TABLE `withdrawals` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `user_id` bigint unsigned NOT NULL,
  `amount` decimal(10,2) NOT NULL,
  `account_type` varchar(20) NOT NULL DEFAULT '',
  `account_no` varchar(100) NOT NULL DEFAULT '',
  `real_name` varchar(50) NOT NULL DEFAULT '',
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '0待审核1审核通过2已拒绝',
  `audit_time` datetime DEFAULT NULL,
  `audit_user_id` bigint unsigned DEFAULT NULL,
  `remark` varchar(255) DEFAULT NULL,
  `created_at` datetime NOT NULL,
  `updated_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 系统配置表
CREATE TABLE `configs` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `key` varchar(50) NOT NULL DEFAULT '',
  `value` text,
  `description` varchar(255) DEFAULT NULL,
  `created_at` datetime NOT NULL,
  `updated_at` datetime NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_key` (`key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 初始化管理员账号（密码：123456）
INSERT INTO `users` (`phone`, `password`, `nickname`, `avatar`, `role`, `balance`, `status`, `created_at`, `updated_at`) VALUES
('13800138000', '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', '管理员', '', 4, 0.00, 1, NOW(), NOW());

-- 初始化游戏分类
INSERT INTO `categories` (`name`, `icon`, `sort`, `status`, `created_at`) VALUES
('王者荣耀', '', 1, 1, NOW()),
('和平精英', '', 2, 1, NOW()),
('英雄联盟', '', 3, 1, NOW()),
('原神', '', 4, 1, NOW());
```

---

*文档版本: v1.0  
*创建日期: 2026-05-21*
